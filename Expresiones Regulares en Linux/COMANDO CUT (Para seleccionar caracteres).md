# COMANDO CUT (Para seleccionar caracteres)

Sirve para cortar determinados caracteres de una cadena de texto, por ejemplo podemos mostrar la primera letra, o hasta la segunda o la tercera:  O podemos mostrar solo la tercera letra por ejemplo:  También podemos mostrar un rango de caracteres, desde el primero al décimo:  O desde el dígito cuarto al décimo:  Aunque hay que entender que cut extrae los dígitos de todas las líneas, por ejemplo aquí tenemos la prueba en un archivo de dos líneas extrayendo los dígitos 4 al 10:  Aunque también podemos filtrar por columnas si marcamos un símbolo delimitador. Por ejemplo en este caso podemos hacer que utilice la coma como referencia y luego nos imprima la segunda columna:&#x20;
