# COMANDO TR (Para sustituir caracteres por otros, pero siempre que tengan la misma extensión; o elimi

Sustituimos los 3 primeros caracteres por AAA:  Con el comando tr se modifican todos los patrones que le indiquemos, por ejemplo, aquí elimina todas las letras l y a y las sustituye por pf: &#x20;

### CONVERTIR TEXTO VERTICAL A ORIZONTAL

También podemos convertir una cadena de texto vertical a horizontal, sustituyedo el \n con un espacio por ejemplo, donde tenemos estas líneas:  Si queremos convertirlo a horizontal, lo haremos así:&#x20;
